{
  "name": "pulseboard-api",
  "private": true,
  "scripts": {
    "dev": "json-server --watch db.json --host 0.0.0.0 --port 3001",
    "start": "json-server --watch db.json --host 0.0.0.0 --port 3001",
    "start:render": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
